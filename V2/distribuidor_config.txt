# Configuración de Distribuidor.py (hot-reload)
# Líneas vacías o que empiezan por # se ignoran
# Formato: clave=valor

# Ruta a Common\Files (dejar vacío para usar ruta por defecto de MetaTrader)
common_files_dir=

# Nombre del fichero maestro
master_filename=Master.txt

# Lista de workers (una línea por worker)
worker_id=01
worker_id=02
worker_id=03
worker_id=04
worker_id=05
worker_id=06

# Intervalo de sondeo en segundos
poll_seconds=1.0

# Intervalo de recarga de config en minutos
reload_minutes=15

